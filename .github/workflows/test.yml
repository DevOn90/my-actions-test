name: Test Workflow

on:
    workflow_dispatch:
        
jobs:
    test-job:
        runs-on: ubuntu-latest
        steps:
            - name: print-hello-world
              shell: bash
              run: echo "Hello, World!"